---
import ScrapSheet from "../components/ScrapSheet.jsx";

const base = import.meta.env.BASE_URL ?? "/";
const basePath = base.endsWith("/") ? base : `${base}/`;
---

<ScrapSheet client:load backHref={basePath} />
